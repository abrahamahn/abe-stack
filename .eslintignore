# Build and output directories
dist/
build/
node_modules/
coverage/
public/assets/

# Config files (except our own ESLint config)
*.config.js
*.config.ts
!.eslintrc.js

# Generated files
*.d.ts
!src/types/**/*.d.ts

# Test files (these are handled with special rules in .eslintrc.js)
# We don't completely ignore them, just apply different rules

# Cache and temporary files
.cache/
.temp/ 