name: CI

on:
  push:
    branches: [main, dev]
  pull_request:
  workflow_dispatch:
    inputs:
      full_build:
        description: 'Run full build (includes all tests)'
        required: false
        default: false
        type: boolean
  schedule:
    - cron: '0 2 * * *'

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

permissions:
  contents: read

jobs:
  setup:
    runs-on: ubuntu-latest
    outputs:
      is_full_build: ${{ steps.check-mode.outputs.is_full_build }}
    steps:
      - name: Determine CI Mode
        id: check-mode
        env:
          EVENT_NAME: ${{ github.event_name }}
          FULL_BUILD_INPUT: ${{ github.event.inputs.full_build }}
        run: |
          if [[ "$EVENT_NAME" == "push" ]] || \
             [[ "$EVENT_NAME" == "schedule" ]] || \
             [[ "$FULL_BUILD_INPUT" == "true" ]]; then
            echo "is_full_build=true" >> "$GITHUB_OUTPUT"
          else
            echo "is_full_build=false" >> "$GITHUB_OUTPUT"
          fi

  global-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5

      - name: Setup pnpm
        uses: pnpm/action-setup@41ff72655975bd51cab0327fa583b6e92b6d3061
        with:
          version: 10.26.2

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Check file headers
        run: pnpm -w run sync:headers:check

      - name: Format Check
        run: pnpm format:check:ci

      - name: Lint
        run: pnpm lint

      - name: Build Storybook (validate stories compile)
        run: pnpm --filter @bslt/storybook build-storybook --quiet

  workspace-build:
    runs-on: ubuntu-latest
    needs: global-checks
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5

      - name: Setup pnpm
        uses: pnpm/action-setup@41ff72655975bd51cab0327fa583b6e92b6d3061
        with:
          version: 10.26.2

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Cache Turbo
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830
        with:
          path: node_modules/.cache/turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}-workspace-build
          restore-keys: |
            ${{ runner.os }}-turbo-

      - name: Build workspace once
        run: pnpm exec turbo run build --output-logs=errors-only

      - name: Upload workspace build artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: workspace-dist
          path: |
            main/**/dist
            main/**/*.tsbuildinfo
          if-no-files-found: error
          retention-days: 1

  sanity-checks:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    needs: workspace-build
    strategy:
      fail-fast: false
      matrix:
        package:
          [
            '@bslt/shared',
            '@bslt/db',
            '@bslt/server',
            '@bslt/web',
            '@bslt/api',
            '@bslt/client-engine',
            '@bslt/react',
            '@bslt/ui',
          ]
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5

      - name: Setup pnpm
        uses: pnpm/action-setup@41ff72655975bd51cab0327fa583b6e92b6d3061
        with:
          version: 10.26.2

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Download workspace build artifacts
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
        with:
          name: workspace-dist
          path: .

      - name: Cache Turbo
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830
        with:
          path: node_modules/.cache/turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}-${{ matrix.package }}
          restore-keys: |
            ${{ runner.os }}-turbo-

      - name: Type Check
        run: pnpm --filter ${{ matrix.package }} type-check

      - name: Run Tests
        run: pnpm --filter ${{ matrix.package }} test

  e2e-tests:
    runs-on: ubuntu-latest
    needs: [setup, global-checks, workspace-build, sanity-checks]
    if: needs.setup.outputs.is_full_build == 'true'
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5

      - name: Setup pnpm
        uses: pnpm/action-setup@41ff72655975bd51cab0327fa583b6e92b6d3061
        with:
          version: 10.26.2

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Playwright Browsers
        run: pnpm exec playwright install --with-deps

      - name: Run E2E Tests
        run: pnpm test:e2e

      - name: Upload E2E Reports
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30

  build-and-verify:
    runs-on: ubuntu-latest
    needs: [setup, global-checks, workspace-build, sanity-checks]
    if: needs.setup.outputs.is_full_build == 'true'
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5

      - name: Setup pnpm
        uses: pnpm/action-setup@41ff72655975bd51cab0327fa583b6e92b6d3061
        with:
          version: 10.26.2

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run CI Verify (governance checks)
        run: pnpm audit:api-governance:strict

  lighthouse:
    runs-on: ubuntu-latest
    needs: [setup, build-and-verify]
    if: needs.setup.outputs.is_full_build == 'true'
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5

      - name: Setup pnpm
        uses: pnpm/action-setup@41ff72655975bd51cab0327fa583b6e92b6d3061
        with:
          version: 10.26.2

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build web app
        run: pnpm --filter @bslt/web build

      - name: Run Lighthouse CI
        run: npx @lhci/cli@0.14.x autorun
        env:
          LHCI_GITHUB_APP_TOKEN: ${{ secrets.LHCI_GITHUB_APP_TOKEN }}

  docker-build-publish:
    runs-on: ubuntu-latest
    needs: build-and-verify
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5

      - name: Resolve image variables
        id: image-vars
        run: |
          REGISTRY="${{ vars.REGISTRY || secrets.REGISTRY || 'ghcr.io' }}"
          IMAGE_NAME_RAW="${{ vars.IMAGE_NAME || github.event.repository.name }}"
          if [[ "$IMAGE_NAME_RAW" != */* ]]; then
            IMAGE_NAME="${{ github.repository_owner }}/$IMAGE_NAME_RAW"
          else
            IMAGE_NAME="$IMAGE_NAME_RAW"
          fi
          API_TAG="${REGISTRY}/${IMAGE_NAME}:${GITHUB_SHA}-api"
          WEB_TAG="${REGISTRY}/${IMAGE_NAME}:${GITHUB_SHA}-web"
          echo "registry=$REGISTRY" >> "$GITHUB_OUTPUT"
          echo "image_name=$IMAGE_NAME" >> "$GITHUB_OUTPUT"
          echo "api_tag=$API_TAG" >> "$GITHUB_OUTPUT"
          echo "web_tag=$WEB_TAG" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f

      - name: Log in to Container Registry
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9
        with:
          registry: ${{ steps.image-vars.outputs.registry }}
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_PASSWORD }}

      - name: Build and push API image
        id: build-api
        uses: docker/build-push-action@ca052bb54ab0790a636c9b5f226502c73d547a25
        with:
          context: .
          file: ./infra/docker/Dockerfile
          push: true
          tags: ${{ steps.image-vars.outputs.api_tag }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push Web image
        id: build-web
        uses: docker/build-push-action@ca052bb54ab0790a636c9b5f226502c73d547a25
        with:
          context: .
          file: ./infra/docker/Dockerfile.web
          push: true
          tags: ${{ steps.image-vars.outputs.web_tag }}
          build-args: |
            VITE_API_URL=
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Write deployment image artifact
        run: |
          cat > deployment-images.json << EOF
          {
            "commit_sha": "${GITHUB_SHA}",
            "registry": "${{ steps.image-vars.outputs.registry }}",
            "image_name": "${{ steps.image-vars.outputs.image_name }}",
            "api_image": "${{ steps.image-vars.outputs.registry }}/${{ steps.image-vars.outputs.image_name }}@${{ steps.build-api.outputs.digest }}",
            "web_image": "${{ steps.image-vars.outputs.registry }}/${{ steps.image-vars.outputs.image_name }}@${{ steps.build-web.outputs.digest }}",
            "api_tag": "${{ steps.image-vars.outputs.api_tag }}",
            "web_tag": "${{ steps.image-vars.outputs.web_tag }}"
          }
          EOF

      - name: Upload deployment image artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: deployment-images
          path: deployment-images.json
          retention-days: 7
