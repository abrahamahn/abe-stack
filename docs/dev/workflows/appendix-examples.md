# Workflow Appendix: Examples

## Example Prompt (Synchronous)

"Implement user authentication for /api/login with bcrypt and JWT, follow existing error handling, add rate limiting, and write integration tests. Work step-by-step and show each change before proceeding."

## Example Prompt (Autonomous)

"Add Vim keybindings to the editor. Use existing keybindings pattern, add tests, and run pnpm lint + pnpm test. Work in checkpoints and report progress."
