// packages/admin/src/routes.test.ts
/*
 * Admin Routes Tests
 *
 * Integration tests for admin routes are handled in the integration test suite.
 * @see src/__tests__/integration/auth-routes.integration.test.ts
 */

import { describe, expect, it } from 'vitest';

describe('admin routes', () => {
  it('should be tested through integration tests', () => {
    // Routes are validated through integration tests
    // This placeholder ensures the test file is valid
    expect(true).toBe(true);
  });
});
