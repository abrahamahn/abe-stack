// src/core/constants/ui.defaults.ts

/**
 * @file UI Defaults
 * @description Default values for UI customization and theming.
 * @module Core/Constants/UIDefaults
 */

export const DEFAULT_THEME = 'system' as const;
export const DEFAULT_DENSITY = 'normal' as const;
export const DEFAULT_CONTRAST_MODE = 'system' as const;
