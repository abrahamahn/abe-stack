// main/shared/src/engine/jobs/storage.ts
// Backward-compatibility stub — canonical location is engine/files/storage.ts

export {
  ALLOWED_IMAGE_TYPES,
  generateUniqueFilename,
  joinStoragePath,
  MAX_IMAGE_SIZE,
  MAX_LOGO_SIZE,
  normalizeStoragePath,
  validateFileType,
} from '../files/storage';
