// src/shared/src/core/guard.ts
/**
 * Guard Utilities — Re-export barrel
 *
 * Canonical source: ../utils/guard.ts (L1)
 */

export {
  assert,
  assertDefined,
  assertNever,
  isAuthenticatedRequest,
  isNonEmptyString,
  isNumber,
  isObjectLike,
  isPlainObject,
  isSafeObjectKey,
  isString,
} from '../utils/guard';
